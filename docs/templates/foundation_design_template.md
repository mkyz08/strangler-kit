# 共通基盤設計書

- 作成: SKILL `scaffold-foundation`
- 作成日:
- 参照: `docs/00_migration_plan.md`（特に §11 未確定事項）, `docs/01_legacy_architecture_overview.md`

Wave 1（画面移行）着手前に一度だけ確定させる、全画面共通の技術方針を記載する。個別画面の設計書
（`02_screen_design.md` `03_api_design.md`）はここで決めた方針に従う。

## 1. 認証・認可方式

- 現行方式（セッション方式）から変更するか、踏襲するか:
- トークン方式を採用する場合: 保存場所（httpOnly Cookie / メモリ等）、リフレッシュ方式:
- 新旧並行稼働期間中のCookie/セッション競合対策（ドメイン分離、パス分離、リバースプロキシ振り分け等）:
- フロントエンド側の認可ガード方針（未ログイン時のリダイレクト、権限による表示制御の共通実装）:

## 2. バックエンド共通方針

- ビルドツール・バージョン（Maven/Gradle、Java、Spring Boot）:
- パッケージ構成:
- 共通例外ハンドリング方式（`@ControllerAdvice`等）とエラーレスポンス形式:
- DBマイグレーションツール（Flyway/Liquibase等）:
- 共通バリデーション方針:

## 3. フロントエンド共通方針

- ビルドツール（Vite等）・React/Redux Toolkitバージョン:
- ディレクトリ構成（機能単位/レイヤー単位等）:
- ルーティング方針（React Router等）:
- 状態管理方針: `createSlice`/`createAsyncThunk` を基本とするか、RTK Queryを採用するか（採用する場合はどの範囲か）:
- 共通APIクライアント設計（ベースURL、認証ヘッダ付与、エラーハンドリングの共通化）:
- 共通レイアウト・共通コンポーネント（ヘッダー、ナビゲーション、エラー表示等）:
- テスト基盤（Jest/React Testing Library、E2E: Playwright等）:

## 4. CI/CD・インフラ

- CI/CDツール:
- デプロイ先・環境（開発/検証/本番）:
- 新旧並行稼働時のルーティング方式（画面単位でのカットオーバーをどう実現するか）:

## 5. 未確定事項

- [ ]
