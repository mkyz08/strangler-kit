# 画面設計書: <SCR-ID> <画面名>

- 作成エージェント: `screen-designer`
- 作成日:
- 参照した現状分析書: `01_legacy_analysis.md`

## 1. 画面基本情報

- 画面ID / 画面名:
- Route（React Router想定パス）:
- 現行との対応:

## 2. 画面レイアウト概要

（ワイヤーフレームの説明、または添付画像へのリンク。主要な表示ブロック・入力ブロック・アクションボタンの配置）

## 3. コンポーネント構成

React コンポーネントツリーを記載する。

```
<SCR-ID_画面名>Page
├── <XxxHeader>
├── <XxxForm>
│   ├── <XxxInputField>
│   └── ...
└── <XxxTable>
```

| コンポーネント | 責務 | Props（主要なもの） |
|---|---|---|
| | | |

## 4. 状態管理（Redux）

- Slice名:
- State shape:

```ts
interface XxxState {
  // ...
}
```

- Actions / Reducers:

| Action | 発火タイミング | Reducerでの処理 |
|---|---|---|
| | | |

- 非同期処理（`createAsyncThunk` または RTK Query）:

| 処理 | 呼び出すAPI | pending/fulfilled/rejectedの扱い |
|---|---|---|
| | | |

- Selector:

| Selector | 用途 |
|---|---|
| | |

## 5. 使用API一覧

詳細は `03_api_design.md` を参照。ここでは画面から見た利用一覧のみ記載する。

| No | 用途 | Method | Path | 対応API設計書項番 |
|---|---|---|---|---|
| | | | | |

## 6. 入力バリデーション仕様

現状分析書「4. 入力項目・バリデーション一覧」を踏襲しつつ、フロントエンド側での実装方針（即時バリデーション/送信時バリデーション、使用ライブラリ等）を記載する。

| 項目名 | ルール | エラーメッセージ | 実施タイミング |
|---|---|---|---|
| | | | |

## 7. 画面遷移

現状分析書「2. 画面遷移」を踏襲し、新画面（Route）に置き換えて記載する。

## 8. アクセス制御・権限

## 9. エラーハンドリング方針

- APIエラー（4xx/5xx）時の表示方針:
- 通信断・タイムアウト時の挙動:

## 10. 現行との差分・UX変更点

現行画面から意図的に変更する点（UI刷新、操作性改善等）を明記する。業務結果に影響する変更がある場合は必ず記載し、レビュー時に指摘できるようにする。

## 11. 未確定事項

- [ ]
